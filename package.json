{
  "name": "farsantes",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build_old": "npm run build --prefix packages/common && npm run build --prefix packages/backend && npm run build --prefix packages/frontend",
    "build": "npm run build --prefix packages/common && npm run build --prefix packages/backend && npm run build --prefix packages/frontendR/farsa",
    "start": "npm start --prefix packages/backend"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "vite": "^6.0.5"
  }
}