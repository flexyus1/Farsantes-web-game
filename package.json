{
  "name": "farsantes",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build_old": "npm run build --prefix packages/common && npm run build --prefix packages/backend && npm run build --prefix packages/frontend",
    "build": "npm run build --prefix packages/common && npm run build --prefix packages/backend && npm run build --prefix packages/frontend",
    "start": "npm start --prefix packages/backend"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@types/react-dom": "^19.0.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "vite": "^6.0.5"
  },
  "devDependencies": {
    "@types/react": "^19.0.8",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.3"
  }
}